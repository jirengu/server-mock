"use strict";

var cozTmpl = require('coz-tmpl');

module.exports = cozTmpl.indexJsBud({
    dirname: __dirname,
    desc: 'Utility functions for copying.',
    module: 'coz-bud/lib/copying'
});


if (!module.parent) {
    require('coz').render(__filename);
}