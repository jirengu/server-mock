/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

"use strict";


var Bud = require('./bud'),
    create = require('./create');

var lib = create.bind(this);

lib.create = create;
lib.Bud = Bud;

module.exports = lib;
