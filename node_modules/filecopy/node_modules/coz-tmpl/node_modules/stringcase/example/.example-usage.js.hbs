var stringcase = require('stringcase');

{{#each usages}}
//------------------------
// Convert to {{name}}
//------------------------
{{#each samples}}stringcase.{{../name}}('{{src}}'); // => "{{converted}}"
{{/each}}


{{/each}}