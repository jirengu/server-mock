/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

"use strict";

const IfType = require('./iftype'),
    is = require('./is'),
    create = require('./create');

let lib = create.bind();
lib.create = create;
lib.IfType = IfType;

lib.is = is;
{{#each types}}
lib.is{{pascalcase this}} = is.bind(lib, '{{this}}');
{{/each}}

module.exports = lib;
