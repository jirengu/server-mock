#!/usr/bin/env/node

var expandglob = require('expandglob');

expandglob([
    'lib/*.js',
    'doc/**/.js'
], {
    cwd: process.cwd(),
    ignore:[]
}, function (err, filenames) {
    console.log(filenames);
});
